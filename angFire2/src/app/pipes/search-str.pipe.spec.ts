import { SearchStrPipe } from './search-str.pipe';

describe('SearchStrPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStrPipe();
    expect(pipe).toBeTruthy();
  });
});
